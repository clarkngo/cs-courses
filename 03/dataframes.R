studentID <- c(1, 2, 3, 4)
studentName <- c("Ezra", "Guille", "Alexa", "Zach")
age <- c(13, 12, 14, 14)
grade <- c("A", "C", "B", "F")

studentData <- data.frame(studentID, studentName, age, grade)
print(studentData)